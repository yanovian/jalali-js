import { buildLocaleConfig, searchLocaleOptions } from './theme.js';

export const localeConfigs = {
  root: buildLocaleConfig('root'),
  fa: buildLocaleConfig('fa'),
};

export { searchLocaleOptions };
